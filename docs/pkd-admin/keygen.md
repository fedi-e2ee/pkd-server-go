# pkd-admin keygen

The `keygen` command generates fresh secret keys for a new PKD server.

**Usage**:

`pkd-admin keygne [flags]`

**Flags**:

- `--config`: config file (default is ./config.yaml)
- `--force`: overwrite keys if they already exist
- `--help`: help for crypto-shred
